[package]
name = "ellipsoid-core"
description = "Ellipsoid geometry generation and flat-pattern unrolling."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
glam.workspace = true
serde.workspace = true
# Settings JSON is core's own format: every front-end reads and writes it, and
# only core knows the quirks (BOM tolerance, defaults for missing fields).
serde_json.workspace = true
